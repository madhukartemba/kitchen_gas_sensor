esphome:
  name: kitchen-gas-alarm-${device_id}
  friendly_name: Kitchen Gas Alarm ${device_id}
  # Include your custom C++ header
  includes:
    - SensorProcessor.h
  on_boot:
    - priority: -10
      then:
        - lambda: |-
            get_processor()->setAbsoluteThreshold(id(gas_threshold).state);
            get_processor()->setRelativeThreshold(id(gas_rel_threshold).state);

esp32:
  board: esp32-c3-devkitm-1
  framework:
    type: esp-idf

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

logger:
api:
ota:
  - platform: esphome

# 1. Analog sensor (MQ-6) - Exposed to HA
sensor:
  - platform: adc
    pin: GPIO0
    name: "MQ6 Gas Level"
    id: gas_sensor
    update_interval: 1s
    attenuation: 12db
    filters:
      - sliding_window_moving_average:
          window_size: 10
          send_every: 5

# 3. Sensor State - Exposed to HA
text_sensor:
  - platform: template
    name: "Gas Sensor State"
    id: sensor_state
    update_interval: 1s
    lambda: |-
      switch(get_processor()->getState()) {
        case WARMUP: return std::string("Warmup");
        case NORMAL: return std::string("Normal");
        case ALARM: return std::string("Alarm");
        case COOLDOWN: return std::string("Cooldown");
        case ERROR: return std::string("Error");
      }
      return std::string("Unknown");

# Threshold slider (Home Assistant)
number:
  - platform: template
    name: "Gas Threshold"
    id: gas_threshold
    min_value: 0.0
    max_value: 3.3
    step: 0.01
    initial_value: 1.5
    restore_value: true
    optimistic: true
    on_value:
      then:
        - lambda: "get_processor()->setAbsoluteThreshold(x);"

  - platform: template
    name: "Gas Relative Threshold"
    id: gas_rel_threshold
    min_value: 0.0
    max_value: 3.3
    step: 0.01
    initial_value: 0.3
    restore_value: true
    optimistic: true
    on_value:
      then:
        - lambda: "get_processor()->setRelativeThreshold(x);"

# 🔊 Buzzer (Internal only)
output:
  - platform: gpio
    pin: GPIO4
    id: buzzer_out

switch:
  - platform: output
    name: "Buzzer"
    id: buzzer
    output: buzzer_out
    internal: true # Hides from Home Assistant

# 🌈 NeoPixel (Internal only)
light:
  - platform: esp32_rmt_led_strip
    pin: GPIO5
    num_leds: 1
    rgb_order: GRB
    chipset: ws2812
    name: "Status LED"
    id: status_led
    internal: true # Hides from Home Assistant

# 2. Virtual reset button - Exposed to HA
button:
  - platform: template
    name: "Reset Alarm"
    id: reset_button_virtual
    on_press:
      then:
        - lambda: 'get_processor()->resetAlarm(millis());'

# 🔘 Physical reset button
binary_sensor:
  - platform: gpio
    pin:
      number: GPIO3
      mode: INPUT
      inverted: false
    name: "Reset Button Physical"
    id: reset_button_phys
    internal: true # Hide the raw button presses from HA
    filters:
      - delayed_on: 10ms # Debounce
    on_press:
      then:
        # Wait 1 second to verify if it's a long press
        - delay: 1s
        - if:
            condition:
              binary_sensor.is_on: reset_button_phys
            then:
              - lambda: 'get_processor()->resetAlarm(millis());'
              - logger.log: "Alarm reset via long press"

# 🚨 Primary Engine Logic
interval:
  - interval: 1s
    then:
      - lambda: |-
          // 1. Send the latest reading to the processor
          get_processor()->update(id(gas_sensor).state, millis());
          
          // 2. Fetch the state
          SensorState current_state = get_processor()->getState();

          // 3. Handle physical outputs natively
          if (current_state == ALARM) {
            id(buzzer).turn_on();
            auto call = id(status_led).turn_on();
            call.set_rgb(1.0, 0.0, 0.0); // Red
            call.set_brightness(1.0);
            call.perform();
          } else if (current_state == WARMUP) {
            id(buzzer).turn_off();
            auto call = id(status_led).turn_on();
            call.set_rgb(1.0, 1.0, 0.0); // Yellow
            call.set_brightness(0.3);
            call.perform();
          } else if (current_state == COOLDOWN) {
            id(buzzer).turn_off();
            auto call = id(status_led).turn_on();
            call.set_rgb(0.0, 0.0, 1.0); // Blue
            call.set_brightness(0.3);
            call.perform();
          } else { // Normal
            id(buzzer).turn_off();
            auto call = id(status_led).turn_on();
            call.set_rgb(0.0, 1.0, 0.0); // Green
            call.set_brightness(0.3);
            call.perform();
          }